### Why `if not name` is used
- It checks if the input is **empty or missing** (`""` or `None`).  
- Raises a `PlantError` if the name is invalid.  
- Ensures the garden only contains **valid plant names**.
